import styled from 'styled-components';
import { FlexColumn } from '../containers/FlexColum.styled';

export const SecretCodeContainer = styled(FlexColumn)`
  padding: 1rem 1.5rem;
  border: 1px solid var(--color-black);
  border-radius: 0.25rem;
`;
